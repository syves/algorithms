function makePairs(s){
   notEqual = 0;
    var numPairs = Math.floor(s.length/ 2)
    //'abc dba'
    for (var offset = 0; offset < numpairs; offset++) {
      [s[offset], s[s.length - 1 - offset]]

          notEqual += 1;
        }
      console.log('left: ' + left[h] + ' right: ' + right[i]);
      }
    return notEqual
    }
    //return notEqual;
}

console.log(MakePairs('abcdba'));

//why isn't this first loop incrementing h?
//
//how could you make a series of pairs from these arrays?
//
// l = 'abd' r = 'deb', r.split().reverse().join() => rev_r = 'bed
//
// pairs = [[a,b]  [b,e] [d,d]] or [(a,b), (b,e), (d,d)] or ['a,b'ect..
// for each p in pairs
// if (pair[0] === pair[1]){
//  return decrement
// } else {
  // if a < b...
  // //decChar(pair[idx]) 

  //else{
    //decChar(pair[idx]) 
  //decrement += 1
  //}
//}
